console.log("testing");
// stub
const pet = {
    
    name: null,
    age: 0,
    hunger: 0,
    energy: 0,
    boredom: 0,
    
    generatePet(event){
        // onClick -> create new pet, give starting HEB/age/name
        // on generate, prompt to input custom name
    }
};

const stats = {

    const hunger = {
        // scale 0-10, 0 = dead
        // -1 every 30s
    },

    const energy = {
        // scale 0-10, 0 = dead
        // -1 every 30s
    },

    const boredom = {
        // scale 0-10, 10 = run away
        // +1 every 30s
    },
};


const game = {

    munch(){
        // button, onClick -> feeds pet, adjusts Hunger
        // +1.5 hunger, +.025 energy
    },  
    
    nap(){
        // button, onClick -> pet sleeps, adjusts Energy
        // +1.5 energy, +.025 boredom
    },
    
    rumpus(){
        // button, onClick -> pet plays, adjusts Boredom
        // -1.5 bordeom, -0.25 energy
    },
};